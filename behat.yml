default:
  suites:
    default:
      contexts:
        - FeatureContext
        - Behat\MinkExtension\Context\MinkContext

  extensions:
    Behat\MinkExtension:
      base_url:  http://smarthire_vue:5174
      sessions:
        default:
          selenium2:
            wd_host: http://selenium:4444/wd/hub
            capabilities: { "browser": "chrome", "version": "*", "extra_capabilities": { "chromeOptions": { "args": ["--disable-gpu", "--no-sandbox"] } } }
