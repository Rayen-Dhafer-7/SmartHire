default:
  suites:
    default:
      contexts:
        - FeatureContext

  extensions:
    Behat\MinkExtension:
      base_url: http://172.21.0.1:5174
      browser_name: chrome
      sessions:
        default:
          selenium2:
            wd_host: http://smarthire_selenium:4444/wd/hub
            capabilities:
              browserName: chrome
              extra_capabilities:
                chromeOptions:
                  args:
                    - "--headless"
                    - "--no-sandbox"
                    - "--disable-dev-shm-usage"
                    - "--disable-gpu"
                    - "--window-size=1920,1080"