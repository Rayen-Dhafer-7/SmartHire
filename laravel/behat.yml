default:
  suites:
    default:
      contexts:
        - FeatureContext

  extensions:
    Behat\MinkExtension:
      base_url: http://smarthire_vue:5174
      sessions:
        default:
          selenium2:
            wd_host: http://selenium:4444/wd/hub
            browser: chrome
            capabilities:
              version: "*"
              platform: LINUX
              extra_capabilities:
                chromeOptions:
                  args:
                    - --disable-gpu
                    - --no-sandbox
                    - --headless
                    - --window-size=1920,1080