name: SmartHire CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  laravel-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # --- Env and dirs (CI only; no changes committed) ---
      - name: Prepare env and dirs for CI
        run: |
          cp laravel/.env.example laravel/.env
          echo "JWT_SECRET=ci-secret-key-for-behat-tests-only" >> laravel/.env
          echo "VITE_API_URL=http://placeholder:8000" > vue/.env
          echo "FRONTEND_URL=http://placeholder:5174" >> vue/.env
          mkdir -p certs
          mkdir -p laravel/certs

      - name: Configure Aiven MySQL and SSL cert for CI
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_PORT: ${{ secrets.DB_PORT }}
          DB_DATABASE: ${{ secrets.DB_DATABASE }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_SSL_CA: ${{ secrets.DB_SSL_CA }}
        run: |
          if [ -n "$DB_HOST" ] && [ -n "$DB_DATABASE" ]; then
            echo "Configuring Laravel for Aiven MySQL..."
            echo "" >> laravel/.env
            echo "DB_CONNECTION=mysql" >> laravel/.env
            echo "DB_HOST=$DB_HOST" >> laravel/.env
            echo "DB_PORT=${DB_PORT:-3306}" >> laravel/.env
            echo "DB_DATABASE=$DB_DATABASE" >> laravel/.env
            echo "DB_USERNAME=$DB_USERNAME" >> laravel/.env
            echo "DB_PASSWORD=$DB_PASSWORD" >> laravel/.env
            echo "MYSQL_ATTR_SSL_CA=/var/www/html/certs/ca.pem" >> laravel/.env
            if [ -n "$DB_SSL_CA" ]; then
              echo "$DB_SSL_CA" > laravel/certs/ca.pem
              echo "Aiven SSL cert written to laravel/certs/ca.pem"
            fi
          else
            echo "Aiven secrets not set (DB_HOST/DB_DATABASE); Laravel will use .env.example (SQLite)."
          fi

      - name: Install Vue node_modules for CI
        run: |
          cd vue
          npm install --include=dev
          npm install vite@latest --save-dev
          ls node_modules/.bin/vite

      - name: Start services with Docker Compose
        run: docker compose up -d --build --wait

      - name: Show container status
        run: docker compose ps -a

      # Host IP = how the browser in Selenium reaches Vue & Laravel. Old workflow used 172.17.0.1; detect gateway or fallback.
      - name: Set Vue .env and Behat base_url to host IP
        run: |
          HOST_IP=$(docker exec smarthire_selenium ip route show default 2>/dev/null | awk '/default/ {print $3}' | tr -d '[:space:]')
          if [ -z "$HOST_IP" ]; then
            HOST_IP=$(docker exec smarthire_laravel ip route show default 2>/dev/null | awk '/default/ {print $3}' | tr -d '[:space:]')
          fi
          if [ -z "$HOST_IP" ]; then
            HOST_IP=$(docker network inspect $(docker inspect smarthire_selenium -f '{{range $k,$v := .NetworkSettings.Networks}}{{$v.NetworkID}}{{end}}' 2>/dev/null) --format '{{range .IPAM.Config}}{{.Gateway}}{{end}}' 2>/dev/null | tr -d '[:space:]')
          fi
          if [ -z "$HOST_IP" ]; then
            HOST_IP="172.17.0.1"
            echo "Using fallback host IP (old workflow value): $HOST_IP"
          else
            echo "Detected host gateway IP: $HOST_IP"
          fi
          echo "VITE_API_URL=http://${HOST_IP}:8000" > vue/.env
          echo "FRONTEND_URL=http://${HOST_IP}:5174" >> vue/.env
          sed -i "s|base_url:.*|base_url: http://${HOST_IP}:5174|" laravel/behat.yml
          echo "Behat base_url set to: http://${HOST_IP}:5174"
          grep base_url laravel/behat.yml

      - name: Restart Vue to pick up .env
        run: docker compose restart vue

      - name: Prepare Laravel (key, migrate)
        run: |
          docker exec smarthire_laravel php artisan key:generate --force
          docker exec smarthire_laravel php artisan migrate --force

      - name: Seed Behat test users (so login API finds dd@dd.dd and rayendhafer@gmail.com)
        run: |
          docker exec smarthire_laravel php artisan tinker --execute="
            \$pwd = Illuminate\Support\Facades\Hash::make('123456');
            \$cid = (string) Illuminate\Support\Str::uuid();
            \$wid = (string) Illuminate\Support\Str::uuid();
            try {
              Illuminate\Support\Facades\DB::table('companies')->insert(['id'=>\$cid,'companyName'=>'Behat Company','email'=>'dd@dd.dd','password'=>\$pwd,'location'=>'CI','industry'=>'Test','created_at'=>now(),'updated_at'=>now()]);
              echo 'Company dd@dd.dd seeded. ';
            } catch (\Exception \$e) { echo 'Company seed: '.\$e->getMessage().' '; }
            try {
              Illuminate\Support\Facades\DB::table('workers')->insert(['id'=>\$wid,'fullName'=>'Behat Worker','email'=>'rayendhafer@gmail.com','password'=>\$pwd,'location'=>'CI','industry'=>'Test','created_at'=>now(),'updated_at'=>now()]);
              echo 'Worker rayendhafer@gmail.com seeded.';
            } catch (\Exception \$e) { echo 'Worker seed: '.\$e->getMessage(); }
          "

      - name: Wait for Laravel to respond
        run: |
          echo "Waiting for Laravel..."
          code="000"
          for i in $(seq 1 24); do
            code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/ || true)
            if [ "$code" != "000" ]; then
              echo "Laravel responded HTTP $code"
              break
            fi
            echo "Waiting... ($i/24)"
            sleep 5
          done
          if [ "$code" = "000" ]; then
            echo "Laravel did not respond. Container logs:"
            docker compose logs laravel --tail=80
            exit 1
          fi

      - name: Wait for Vue dev server to respond
        run: |
          echo "Polling for Vue on port 5174..."
          code="000"
          for i in $(seq 1 72); do
            code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:5174/ || true)
            if [ "$code" != "000" ]; then
              echo "Vue responded HTTP $code"
              break
            fi
            echo "Waiting for Vue... ($i/72)"
            sleep 5
          done
          if [ "$code" = "000" ]; then
            echo "Vue did not respond. Vue container logs:"
            docker compose logs vue --tail=200
            exit 1
          fi

      # Warm Vue app and wait for SPA to be ready (CI is slower; form must be in DOM before Behat fills it)
      - name: Warm Vue app and wait for login page ready
        run: |
          echo "Warming Vue app (hit /login so dev server and SPA are ready)..."
          curl -s -o /dev/null http://localhost:5174/
          curl -s -o /dev/null http://localhost:5174/login
          echo "Waiting 15s for Vue SPA to be fully ready before Behat..."
          sleep 15

      - name: Run Behat tests
        run: docker exec smarthire_laravel vendor/bin/behat --format=pretty
